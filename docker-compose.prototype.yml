services:
  watermark-api:
    image: python:3.11-slim
    working_dir: /app
    command: python backend/server.py --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  # Production direction:
  # Build one worker image per research repo. Do not install all model deps
  # into watermark-api.
  #
  # sfwmark-worker:
  #   build:
  #     context: .
  #     dockerfile: backend/workers/sfwmark.Dockerfile
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - capabilities: ["gpu"]
  #
  # gaussian-shannon-worker:
  #   build:
  #     context: .
  #     dockerfile: backend/workers/gaussian-shannon.Dockerfile
  #
  # lawa-worker:
  #   build:
  #     context: .
  #     dockerfile: backend/workers/lawa.Dockerfile
